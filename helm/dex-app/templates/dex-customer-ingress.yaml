{{ if .Values.Installation.V1.GiantSwarm.OIDC.Enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ include "resource.default.name.customer" . }}
  labels:
{{ include "dex.labels.customer" . | indent 4 }}
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if .Values.Installation.V1.GiantSwarm.OIDC.Letsencrypt }}
    kubernetes.io/tls-acme: "true"
    {{- end }}
spec:
  tls:
    - hosts:
        - {{ .Values.Installation.V1.GiantSwarm.OIDC.IssuerAddress | quote }}
      {{- if .Values.Installation.V1.GiantSwarm.OIDC.Letsencrypt }}
      secretName: {{ include "resource.default.name.customer" . }}-tls-le
      {{- else }}
      secretName: {{ include "resource.default.name.customer" . }}-tls
      {{- end }}
  rules:
    - host: {{ .Values.Installation.V1.GiantSwarm.OIDC.IssuerAddress | quote }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ include "resource.default.name.customer" . }}
              servicePort: 32000
{{ end }}
