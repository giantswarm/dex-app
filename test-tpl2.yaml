global:
  image:
    registry: custom-global-registry.com
dex:
  image:
    registry: gsoci.azurecr.io
    name: giantswarm/dex-app
    tag: "2.2.0"
    repository: '{{ default .Values.image.registry (default "" .Values.global.image.registry) }}/{{ .Values.image.name }}'
